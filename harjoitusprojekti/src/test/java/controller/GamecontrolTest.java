/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package controller;

import javafx.scene.control.Label;
import javafx.scene.text.Font;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;
import neljansuora.controller.Gamecontrol;

/**
 *
 * @author annika
 */
public class GamecontrolTest {
    /*
    private Label[][] gameArea;
    private Gamecontrol controller;
    
    @Before
    public void setUp() {
        this.controller = new Gamecontrol();
        this.gameArea = new Label[7][6];
        for (int i = 0; i < 6; i++) {
            for (int y = 0; y < 7; y++) {
                this.gameArea[y][i] = new Label(" ");        
            }
        }
    }
    
    
    @Test
    public void getPlayableTileFindsCorrectEmptyTiles() {
        resetGameArea();
        int firstIndex = this.controller.getPlayableTile(this.gameArea, 0);
        assertEquals(5, firstIndex);
    }
    
    private void resetGameArea() {
        
        for (int i = 0; i < 6; i++) {
            for (int y = 0; y < 7; y++) {
                Label currentLabel = new Label(" ");
                this.gameArea[y][i] = currentLabel;         
            }
        }
    }
    */
}
